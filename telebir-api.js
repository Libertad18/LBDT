// telebirr-api.js (Mock version for demo)
function sendToTelebirr(phone, amount) {
  alert(`Send ${amount} birr to developer at 0943838862`);
  // You can use fetch() to send this to a backend that uses the real Telebirr API
}
